#include "holberton.h"

/**
 * _atoi - convert string into integer
 * @s: string
 *
 * Return: if no number in string, return 0. convert otherwise
 */

int _atoi(char *s)
{

	int result, i;

	if (s[0] == '-')
		return 

	for (i = 0; *s != '\0'; i++)
	{
		result = *s - '0' * 10
	}

	return (0);

}

#include <stdio.h>

/**
 * main - Void alphabet - print alphabet x10
 *
 *
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int printX10;

for (printX10 = 0; printX10 <= 10; printX10++)
{
void print_alphabet(void);
}

return (0);
}

/**
 * print_alphabet - Prints a to z in lowercase
 */

void print_alphabet(void)
{
char alphabet;

for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
putchar(alphabet);

putchar('\n');
}

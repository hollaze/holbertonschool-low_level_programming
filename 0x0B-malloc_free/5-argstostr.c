#include <stdio.h>
#include <stddef.h>
#include <stdlib.h>

/**
 * strtow - splits string into words
 *
 * @str: string
 *
 * Return: 0
 */

char *argstostr(int ac, char **av)
{

	if (ac == 0 || av == NULL)
                return (NULL);

return (*av);

}
